version: 0.0.{build}
init:
- cmd: "$env:MYSQL_PWD=\"Password12!\" \n$cmd = '\"C:\\Program Files\\MySql\\MySQL Server 5.7\\bin\\mysql\" -e \"create database spslocal;\" --user=root'\niex \"& $cmd\""
services:
- mysql
before_build:
- ps: nuget restore
build:
  verbosity: minimal